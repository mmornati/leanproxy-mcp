---
version: 1
slug: "overrides-home-html"
primary_target: "overrides/home.html"
related_targets: ["docs/index.md"]
---

Scope: docs site landing page (overrides/home.html, rendered at docs/index.md). Mode: Persuade. Docs pages inherit the world in Read mode.

Audience: individual developers running an AI coding client with several MCP servers. Action: install, or open the measurements. Proof: harness numbers from docs/benchmark-results.md and the README benchmark table, with their caveats.

## Direction contract

THESIS: every MCP call goes through a checkpoint scanner. LeanProxy weighs the baggage (tokens) and flags the threats (secrets, injected text, drifting tools) in the same pass. Refuses the category default: a dark hero, a terminal gif, and a 3x3 grid of feature cards.

OWN-WORLD: baggage X-ray pseudocolor on a pale scanner ground. Translucent amber (organic, meaning tokens), teal-green (mixed, meaning cleared) and cobalt (dense, meaning metal and threats) shapes multiply where they overlap. Threat-red bracket boxes, scanner readout strips in tabular mono, conveyor rails as section dividers. Archivo at expanded width for display, Archivo for text, JetBrains Mono only for code and measurements.

STORY: the visitor sees that their client hauls 10,049 tokens of schemas on every turn, watches LeanProxy scan it down to 318 and box a leaked key, believes it because every number shows its test conditions, then installs.

FIRST VIEWPORT: the headline and the offer sit at top left, with install and measurements CTAs and a copyable brew command. Below it, a full-width scanner band shows the request bag: left of the scan line full schema baggage, right of it the lean router plus a redacted key. A readout strip sits under the band. The scan line is draggable.

FORM: X-ray checkpoint, own grounded list position 3 of 7, seed key 7ac61095 (degraded roll, no challengers). Raise kept from the datasheet pick: every figure carries its test condition.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
